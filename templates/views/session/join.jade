extends ../../layouts/default

block flash-messages
	//- intentionally blank

block content
	.container
		.page-intro
			h1 Join Hacksmiths
			+flash-messages(messages)
		.page-content
			+auth-forms({ modal: true, initial: 'join' })

block js
	script.
		jQuery(function($) {
			$('.auth-form').find('.form-control').eq(0).focus().select();
		});
